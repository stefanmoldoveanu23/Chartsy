[package]
name = "chartsy"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
iced = {version= "0.12.0", features=["canvas", "advanced"]}
iced_runtime = "0.12.0"
iced_style = "0.12.0"
rand = "0.9.0-alpha.0"
mongodb = {version="2.8.1", features=["async-std-runtime"], default-features=false}
svg = "0.15.1"
pwhash = "1.0.0"
directories = "5.0.1"
async-recursion = "1.0.5"
lettre = { version="0.11.4", default-features=false, features = ["smtp-transport", "hostname", "builder", "async-std1-rustls-tls"] }
regex = "1.10.3"